[project]
name = "visual-coach"
version = "0.1.0"
description = "Player video analysis using Gemini API — offense/defense structured report"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
]
